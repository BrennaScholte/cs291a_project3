<h1>Login</h1>

<%= form_with(model: @session, url: login_path, local: true) do |form| %>
  <div>
    <%= form.label :username %>
    <%= form.text_field :username, required: true %>
  </div>

  <%= form.submit "Log In" %>
<% end %>
